import { PrismaClient } from '@prisma/client';
/**
 * Channel 种子数据
 *
 * 为每个 provider 创建一个测试渠道。
 * 注意：需要在 .env 中配置实际的 API Key。
 */
export declare function seedChannels(prisma: PrismaClient): Promise<void>;
//# sourceMappingURL=channels.seed.d.ts.map