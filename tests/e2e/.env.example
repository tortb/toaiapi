# 前端地址
BASE_URL=http://localhost:3000
# 后端 API 地址
API_BASE_URL=http://localhost:3001
# 管理员账号（用于前置数据准备）
ADMIN_EMAIL=admin@toaiapi.com
ADMIN_PASSWORD=Admin@123456
# 测试用户前缀（每次运行自动加时间戳，保证幂等）
TEST_USER_PREFIX=e2e-test
# 测试用户密码
TEST_USER_PASSWORD=TestPass@123
# API Gateway 测试用的真实 API Key（可选，不设置则跳过网关测试）
TEST_API_KEY=
# 测试用的模型名称
TEST_MODEL=gpt-4o-mini
